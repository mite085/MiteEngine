#ifndef MITE_UI_MODULE
#define MITE_UI_MODULE

namespace mite {
//// 供Renderer调用的初始化接口
//void SetupRenderBackend(RendererBackend backend);
//
//// 供MainLoop调用的主接口
//void RenderAllPanels(SceneView &sceneView);
};

#endif
