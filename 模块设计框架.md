Application模块：负责软件的启动和结束，各个子模块的创建与初始化操作，DEMO场景（DefaultScene）的构建，以及MainLoop

Asset模块：负责模型、材质、纹理的加载、管理与缓存。

Command模块（已初步实现，但未集成进引擎）：作为UI和引擎解耦层，负责对Commad进行注册、执行、撤销、重做。

Core模块：负责封装一些公共的功能，包括filesystem用于加载外部文件，logger负责日志记录，thread负责多线程相关，time负责计时，uuid负责生产唯一ID。

Data模块：1. 负责对基本数据进行封装，包括bounding volume、camera、frustum、mesh、model、ray、texture、transform。2. 负责枚举类型的定义，如asset type、bounding volume type、framebuffer type、light type等。3. 负责framebuffer、gbuffer、shader、ubo、ssbo，bindingpoint等着色器相关进行封装。4. 负责camera instance、light instance、material instance、mesh instance这些实例进行封装。

Event模块：事件作为模块与模块间解耦的关键存在，是引擎最底层的依赖，整个引擎的很多操作都是依赖事件驱动的。Event模块实现了Event抽象，和负责生产与分发事件的全局单例EventBus事件总线系统，并实现了同步/异步/延迟分发机制

Input模块：负责外部输入事件（主要为键盘和鼠标事件）的订阅与管理，以及输入栈与输入上下文的抽象

Light模块：负责不同光源（点光源、方向光、聚光灯）的具体实现，以及各自对应的ShadowMap实现，以及LightManager光源管理。

Material模块：负责材质模板抽象和材质工厂（负责根据模板创建材质），以及实现了基于GLTF参数的PBR材质

Renderer模块：渲染模块，实现了渲染管线（目前仅包含ShadowMap -> G-Buffer -> deferred lighting -> forward这四个部分，通过RenderContext传递参数），实现了RenderCommand命令队列，并负责OpenGL渲染方法的封装

SceneCore模块：ECS架构场景组织的核心实现模块，负责Entity与Component的创建、注册和管理，Component抽象和包含MaterialComponent、CameraComponent等的具体实现，以及ComponentSystem负责每帧更新脏组件

SceneGraph模块：负责SceneNode的管理和SceneTree的构建，以及BVH空间加速结构的构建，并基于加速结构实现视锥体剔除和RayCastFirst的简单Pick功能

SceneSerializer模块（未实现）：负责场景的序列号和反序列化，与ECS架构的SceneCore配合，用于场景的保存和加载

SceneView模块：接收来自SceneGraph的视锥体剔除结果之后，负责逐个构建RenderableItem并填充RenderQueue渲染队列

UI模块：负责对ImGui进行二次封装，完成包括ViewPort、SceneTree、Properties的界面绘制

Window模块：负责对GLFW进行二次封装，完成包括窗口创建和管理，Resize等窗口事件响应等功能